{% extends '_layout' %}

{% block body %}
  {% set info = {
    craftVersion: craft.app.getVersion(),
    environment: getenv('ENVIRONMENT')
  } %}

  <hello-world :info="{{ info|json_encode }}">
    {{ getenv('DB_DATABASE') }}
  </hello-world>
{% endblock %}